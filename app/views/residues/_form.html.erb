<%= form_for(residue) do |f| %>
  <% if residue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(residue.errors.count, "error") %> prohibited this residue from being saved:</h2>

      <ul>
      <% residue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, "Nome:" %>
    <%= f.text_field :name %>
  </div>
  
  <div class="field">
    <div class="span4">
      <div class="field">
        <%= f.label :kind, "Tipo:" %>
        <%= f.select :kind, ["Sólido Orgânico","Sólido Inorgânico","Líquido Orgânico","Líquido Inorgânico","Líquido Inflamável","Outros"] %>
      </div>
    </div>
    <div class="span8">
      <div class="field">
        <%= f.label :blend, "Composição:" %>
        <%= f.text_field :blend %>
      </div>
    </div>
  </div>
  
  <div class="field">
  <div class="span4">
    <div class="field">
      <%= f.label :onu, "Código ONU:" %>
      <%= f.text_field :onu %>
    </div>
  </div>
  <div class="span4">
    <div class="field">
      <%= f.label :code, "Código Class:" %>
      <%= f.text_field :code %>
    </div>
  </div>
    <div class="span4">
      <div class="field">
        <%= f.label :laboratory_id, "Laboratório:"  %>
        <%= f.collection_select(:laboratory_id, Laboratory.all.order(:name), :id, :name) %>
      </div>
    </div>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>