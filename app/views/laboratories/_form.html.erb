<%= form_for(laboratory) do |f| %>
  <% if laboratory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(laboratory.errors.count, "error") %> prohibited this laboratory from being saved:</h2>

      <ul>
      <% laboratory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <br>
  
  <div class="field lb_name">
    <%= f.label :name, "Nome: " %>
    <%= f.text_field :name %>
  </div>
  
  <br>
  <div class="field lb_list">
    <%= f.label :department_id, "Departamento: " %>
    <%= f.collection_select(:department_id, Department.all.order(:name), :id, :name) %>
  </div>
  <br>
  

  <div class="field lb_fac">
    <%= f.label :user_id, "Facilitador: " %>
    <%= f.text_field :user_id %>
  </div>
  <br>
  
  <div class="actions">
    <%= f.submit %>
  </div>
  <br>
<% end %>
