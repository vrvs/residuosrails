<p id="notice"><%= notice %></p>
<div class = "title">
  <h1>Registros</h1>
</div>

<table>
  <thead>
    <tr>
      <th>Peso</th>
      <th>Resíduo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @registers.each do |register| %>
      <tr>
        <td><%= register.weight %></td>
        <td><%= Residue.find(register.residue_id).name %></td>
        <td><%= link_to 'Show', register %></td>
        <td><%= link_to 'Edit', edit_register_path(register) %></td>
        <td><%= link_to 'Destroy', register, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Registro', new_register_path,class: 'css_btn_class' %>
<%= link_to 'Voltar', main_adm_path,class: 'css_btn_class' %>
